Number {}
String {
    length: Number
}

type User: Number
type Int: Number

Yellow(id)(meep) {
    id: Number,
    hi: String?,
    meep: Meep(id) {
        id: Number,
    }+,
    user_id: User(id),
}