type Size: Number

model User(id)(name) {
    id: Number,
    name: String
}

model Yellow(id, size) {
    id: Number,
    size: Size,
    meep: model Meep(id) {
        id: Number,
    }+,
    user: User(id),
}