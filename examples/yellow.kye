type Size: Number

model User(id)(name) {
    id: Number,
    name: String
}

model Yellow(id)(meep) {
    id: Number,
    hi: Size?,
    meep: model Meep(id) {
        id: Number,
    }+,
    user_id: User(id),
}